# PostgreSQL
POSTGRES_USER=idcore
POSTGRES_PASSWORD=idcore
POSTGRES_DB=idcore
POSTGRES_HOST_PORT=5432

# Valkey
VALKEY_PASSWORD=valkey
VALKEY_HOST_PORT=6379

# 上流 IdP モック (navikt/mock-oauth2-server)
IDP_MOCK_HOST_PORT=8081

# Adminer (DB GUI)
ADMINER_HOST_PORT=8082
