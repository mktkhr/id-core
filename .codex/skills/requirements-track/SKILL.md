---
name: requirements-track
description: '要求文書の変更履歴と差分を追記する。"requirements-track", "要求差分", "変更履歴更新" 等で発動。'
---
# 要求差分追跡スキル

`docs/requirements/*/index.md` の「変更履歴」セクションを最新化する。

## 前提として読み込む context

**必須** (必ず読む):
- 対象の `docs/requirements/{N}/index.md` (既存「変更履歴」セクションのみで足りる場合は部分読み可)

**条件付き**: なし

**読み込まない**: context/* 配下は不要。差分の追記のみが目的。

詳細は `docs/context/README.md` の対応表を参照。

## 手順

1. 「変更履歴」を読み取る
2. 前回追記以降の変更を抽出する
3. 重複行を除外して追記する
4. 件数と概要を報告する

## 対象

- 業務ルールの変更
- 入出力要件の追加 / 削除
- 例外シナリオの変更
- バリデーション方針の変更
- 権限要件の変更
- 監査 / 非機能要件の変更
